{{ form_start(form) }}
    {% if autosubmit == true %}
        {{ form_widget(form, { 'attr': {'class': 'hidden-row'} }) }}
    {% else %}
        {{ form_widget(form) }}
    {% endif %}
    {#{{ form_widget(form) }}#}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
